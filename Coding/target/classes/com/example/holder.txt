List<String> filteredDangerousList = new ArrayList<>();

            StringBuilder filepathBuilder = new StringBuilder();
            for(int i = 0; i < dangerousFileList.size(); i++ )
            {
                for (String str : dangerousFileList)
                {
                    if (str.contains(path)){
                        filepathBuilder.append(str);
                    }
                    if (filepathBuilder.toString().contains(path) && !filepathBuilder.toString().contains(".")){
                        filepathBuilder.append(str);
                    }
                    if (filepathBuilder.toString().contains(path) && str.contains(".")){
                        filepathBuilder.append(str);
                        break;
                    }
                    else{
                        System.out.println(str + " is not the correct format, it has been deleted");
                        break;
                    }
                }
                filteredDangerousList.set(i, filepathBuilder.toString());
            }
            System.out.println("Filtered Dangerous files: \n");
            for (String s : filteredDangerousList){
                System.out.println(s);
            }